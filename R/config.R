#' @export
TAGGED_INDVIDUAL_FIELDS <- list( # nolint
  tagged_individual = c("id", "is_internal", "is_active", "tagged_date"),
  individual = c("id", "individual_id", "weight", "sex", "total_length"),
  tag = c("id", "serial_number", "comm_protocol", "frequency"),
  sensors = c("id", "sensor_types_id")
)
