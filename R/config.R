#' @export
TAGGED_INDVIDUAL_FIELDS <- list( # nolint
  tagged_individual = c("is_internal", "is_active", "tagged_date"),
  individual = c("individual_id", "weight", "sex", "total_length"),
  tag = c("serial_number", "comm_protocol", "frequency"),
  sensors = c("id", "sensor_types_id")
)
